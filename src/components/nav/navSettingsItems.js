import { routeNames } from '@/router'
const settings = {
    label: 'Settings',
    route_name: routeNames.settings.settings,
}
const settingsUpdateUsername = {
  label: 'Update Username',
  route_name: routeNames.settings.updateUsername,
}

export { settings, settingsUpdateUsername }
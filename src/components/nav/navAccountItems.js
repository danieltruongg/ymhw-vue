import { routeNames } from '@/router'

const signup = {
  label: 'Sign-up',
  route_name: routeNames.signup,
}
const login = {
  label: 'Login',
  route_name: routeNames.login,
}
const profile = {
  label: 'Profile',
  route_name: routeNames.profile,
}

export { signup, login, profile }
